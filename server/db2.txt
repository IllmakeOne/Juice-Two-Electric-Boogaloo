create table suppliers(
id SERIAL NOT NULL PRIMARY KEY,
data json NOT NULL
);
create table cart_lists (
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table barbills(
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table phones(
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table phones(
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table apps(
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table apps(
id SERIAL NOT NULL PRIMARY KEY, 
date VARCHAR (15),
data json NOT NULL
);

create table apps(
id SERIAL NOT NULL PRIMARY KEY, 
data json NOT NULL
);

create table appsly(
id SERIAL NOT NULL PRIMARY KEY,
data json NOT NULL
);

create table suppliers(
id SERIAL NOT NULL PRIMARY KEY,
data json NOT NULL
);